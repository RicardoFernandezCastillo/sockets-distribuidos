<Window x:Class="Sockets_Servidor.histograma_grafico.W_Grafico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Sockets_Servidor.histograma_grafico"
        mc:Ignorable="d"
        Title="W_Grafico" Height="700" Width="1000"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <materialDesign:Card Grid.Row="0" Padding="16" Margin="16" UniformCornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="/image/Logo.png" Height="80" Width="80" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="Monitor Nacional de Almacenamiento" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="Total: 74 TB - Usado: 61 GB - Libre: 13 GB" FontSize="16" Margin="0,5,0,0"/>
                    <TextBlock x:Name="ServidoresArribatxt" Text="Reportando 0 de 9" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="2" ToolTip="Cerrar" HorizontalAlignment="Right" VerticalAlignment="Top" Click="CerrarVentana">
                    <materialDesign:PackIcon Kind="Close" Height="24" Width="24" />
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- Contenido principal -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Card 1: Información del Router -->
            <materialDesign:Card Grid.Column="0" Padding="16" Margin="0,0,8,0" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Router" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <Image x:Name="imgRouter" Source="/image/router-de-wifi.png" Height="80" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <TextBlock x:Name="txtNombreRouter" Text="Nombre del Router" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock x:Name="txtTotalEspacio" Text="Total: 400 GB" FontSize="16" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="txtUsoEspacio" Text="Uso: 24 GB" FontSize="16" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    <TextBlock x:Name="txtLibreEspacio" Text="Libre: 376 GB" FontSize="16" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    <TextBlock x:Name="txtRam" Text="RAM: 8 GB" FontSize="16" HorizontalAlignment="Center" Margin="0,16,0,0"/>
                    <TextBlock x:Name="txtIP" Text="IP: 192.168.1.1" FontSize="16" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Card 2: Historial de Uso de Disco -->
            <materialDesign:Card Grid.Column="1" Padding="16" Margin="8,0,8,0" UniformCornerRadius="8">
                <DockPanel>
                    <TextBlock Text="Historial de Uso de Disco" FontSize="22" FontWeight="Bold" DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <DataGrid x:Name="dgHistorialUso" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
          ItemsSource="{Binding Historial}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="*" />
                            <DataGridTextColumn Header="Hora" Binding="{Binding Hora}" Width="*" />
                            <DataGridTextColumn Header="Uso Disco (TB)" Binding="{Binding UsoDiscoTB, StringFormat='{}{0} %'}" Width="*" />

                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </materialDesign:Card>

            <!-- Card 3: Histograma de Uso -->
            <materialDesign:Card Grid.Column="2" Padding="16" Margin="8,0,0,0" UniformCornerRadius="8">
                <DockPanel>
                    <TextBlock Text="Histograma de Uso" FontSize="22" FontWeight="Bold" DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <lvc:CartesianChart x:Name="histogramaChart">
                        <lvc:CartesianChart.Series>
                            <lvc:ColumnSeries Title="Uso Disco (TB)" Values="{Binding HistogramaValores}" />
                        </lvc:CartesianChart.Series>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Fecha y Hora" Labels="{Binding HistogramaLabels}" />
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Uso Disco (TB)" />
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </DockPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>
